name: Release

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release automation will be enabled after package publishing decisions are made."
