Run the local x402 Testnet demo with `stellar-agent pay x402 <localhost-url> --allow-localhost-demo --json`, verify that it records a receipt, and update docs without implying production facilitator support is complete.
