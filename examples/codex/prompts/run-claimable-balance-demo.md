Create a small Testnet XLM claimable balance with `stellar-agent claimable create --from agent --to <recipient> --amount 0.0000001 --asset XLM --json`, list it with `stellar-agent claimable list --address <recipient> --json`, claim it with `stellar-agent claimable claim --account <recipient-alias> --balance-id <id> --json`, and summarize transaction hashes plus receipt paths without printing secrets.
